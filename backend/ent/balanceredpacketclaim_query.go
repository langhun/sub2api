// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/Wei-Shaw/sub2api/ent/balanceredpacket"
	"github.com/Wei-Shaw/sub2api/ent/balanceredpacketclaim"
	"github.com/Wei-Shaw/sub2api/ent/predicate"
)

// BalanceRedPacketClaimQuery is the builder for querying BalanceRedPacketClaim entities.
type BalanceRedPacketClaimQuery struct {
	config
	ctx           *QueryContext
	order         []balanceredpacketclaim.OrderOption
	inters        []Interceptor
	predicates    []predicate.BalanceRedPacketClaim
	withRedpacket *BalanceRedPacketQuery
	modifiers     []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the BalanceRedPacketClaimQuery builder.
func (_q *BalanceRedPacketClaimQuery) Where(ps ...predicate.BalanceRedPacketClaim) *BalanceRedPacketClaimQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *BalanceRedPacketClaimQuery) Limit(limit int) *BalanceRedPacketClaimQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *BalanceRedPacketClaimQuery) Offset(offset int) *BalanceRedPacketClaimQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *BalanceRedPacketClaimQuery) Unique(unique bool) *BalanceRedPacketClaimQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *BalanceRedPacketClaimQuery) Order(o ...balanceredpacketclaim.OrderOption) *BalanceRedPacketClaimQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryRedpacket chains the current query on the "redpacket" edge.
func (_q *BalanceRedPacketClaimQuery) QueryRedpacket() *BalanceRedPacketQuery {
	query := (&BalanceRedPacketClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(balanceredpacketclaim.Table, balanceredpacketclaim.FieldID, selector),
			sqlgraph.To(balanceredpacket.Table, balanceredpacket.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, balanceredpacketclaim.RedpacketTable, balanceredpacketclaim.RedpacketColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first BalanceRedPacketClaim entity from the query.
// Returns a *NotFoundError when no BalanceRedPacketClaim was found.
func (_q *BalanceRedPacketClaimQuery) First(ctx context.Context) (*BalanceRedPacketClaim, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{balanceredpacketclaim.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *BalanceRedPacketClaimQuery) FirstX(ctx context.Context) *BalanceRedPacketClaim {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first BalanceRedPacketClaim ID from the query.
// Returns a *NotFoundError when no BalanceRedPacketClaim ID was found.
func (_q *BalanceRedPacketClaimQuery) FirstID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{balanceredpacketclaim.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *BalanceRedPacketClaimQuery) FirstIDX(ctx context.Context) int64 {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single BalanceRedPacketClaim entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one BalanceRedPacketClaim entity is found.
// Returns a *NotFoundError when no BalanceRedPacketClaim entities are found.
func (_q *BalanceRedPacketClaimQuery) Only(ctx context.Context) (*BalanceRedPacketClaim, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{balanceredpacketclaim.Label}
	default:
		return nil, &NotSingularError{balanceredpacketclaim.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *BalanceRedPacketClaimQuery) OnlyX(ctx context.Context) *BalanceRedPacketClaim {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only BalanceRedPacketClaim ID in the query.
// Returns a *NotSingularError when more than one BalanceRedPacketClaim ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *BalanceRedPacketClaimQuery) OnlyID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{balanceredpacketclaim.Label}
	default:
		err = &NotSingularError{balanceredpacketclaim.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *BalanceRedPacketClaimQuery) OnlyIDX(ctx context.Context) int64 {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of BalanceRedPacketClaims.
func (_q *BalanceRedPacketClaimQuery) All(ctx context.Context) ([]*BalanceRedPacketClaim, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*BalanceRedPacketClaim, *BalanceRedPacketClaimQuery]()
	return withInterceptors[[]*BalanceRedPacketClaim](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *BalanceRedPacketClaimQuery) AllX(ctx context.Context) []*BalanceRedPacketClaim {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of BalanceRedPacketClaim IDs.
func (_q *BalanceRedPacketClaimQuery) IDs(ctx context.Context) (ids []int64, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(balanceredpacketclaim.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *BalanceRedPacketClaimQuery) IDsX(ctx context.Context) []int64 {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *BalanceRedPacketClaimQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*BalanceRedPacketClaimQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *BalanceRedPacketClaimQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *BalanceRedPacketClaimQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *BalanceRedPacketClaimQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the BalanceRedPacketClaimQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *BalanceRedPacketClaimQuery) Clone() *BalanceRedPacketClaimQuery {
	if _q == nil {
		return nil
	}
	return &BalanceRedPacketClaimQuery{
		config:        _q.config,
		ctx:           _q.ctx.Clone(),
		order:         append([]balanceredpacketclaim.OrderOption{}, _q.order...),
		inters:        append([]Interceptor{}, _q.inters...),
		predicates:    append([]predicate.BalanceRedPacketClaim{}, _q.predicates...),
		withRedpacket: _q.withRedpacket.Clone(),
		// clone intermediate query.
		sql:  _q.sql.Clone(),
		path: _q.path,
	}
}

// WithRedpacket tells the query-builder to eager-load the nodes that are connected to
// the "redpacket" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *BalanceRedPacketClaimQuery) WithRedpacket(opts ...func(*BalanceRedPacketQuery)) *BalanceRedPacketClaimQuery {
	query := (&BalanceRedPacketClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withRedpacket = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		RedpacketID int64 `json:"redpacket_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.BalanceRedPacketClaim.Query().
//		GroupBy(balanceredpacketclaim.FieldRedpacketID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *BalanceRedPacketClaimQuery) GroupBy(field string, fields ...string) *BalanceRedPacketClaimGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &BalanceRedPacketClaimGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = balanceredpacketclaim.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		RedpacketID int64 `json:"redpacket_id,omitempty"`
//	}
//
//	client.BalanceRedPacketClaim.Query().
//		Select(balanceredpacketclaim.FieldRedpacketID).
//		Scan(ctx, &v)
func (_q *BalanceRedPacketClaimQuery) Select(fields ...string) *BalanceRedPacketClaimSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &BalanceRedPacketClaimSelect{BalanceRedPacketClaimQuery: _q}
	sbuild.label = balanceredpacketclaim.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a BalanceRedPacketClaimSelect configured with the given aggregations.
func (_q *BalanceRedPacketClaimQuery) Aggregate(fns ...AggregateFunc) *BalanceRedPacketClaimSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *BalanceRedPacketClaimQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !balanceredpacketclaim.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *BalanceRedPacketClaimQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*BalanceRedPacketClaim, error) {
	var (
		nodes       = []*BalanceRedPacketClaim{}
		_spec       = _q.querySpec()
		loadedTypes = [1]bool{
			_q.withRedpacket != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*BalanceRedPacketClaim).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &BalanceRedPacketClaim{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withRedpacket; query != nil {
		if err := _q.loadRedpacket(ctx, query, nodes, nil,
			func(n *BalanceRedPacketClaim, e *BalanceRedPacket) { n.Edges.Redpacket = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *BalanceRedPacketClaimQuery) loadRedpacket(ctx context.Context, query *BalanceRedPacketQuery, nodes []*BalanceRedPacketClaim, init func(*BalanceRedPacketClaim), assign func(*BalanceRedPacketClaim, *BalanceRedPacket)) error {
	ids := make([]int64, 0, len(nodes))
	nodeids := make(map[int64][]*BalanceRedPacketClaim)
	for i := range nodes {
		fk := nodes[i].RedpacketID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(balanceredpacket.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "redpacket_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (_q *BalanceRedPacketClaimQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *BalanceRedPacketClaimQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(balanceredpacketclaim.Table, balanceredpacketclaim.Columns, sqlgraph.NewFieldSpec(balanceredpacketclaim.FieldID, field.TypeInt64))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, balanceredpacketclaim.FieldID)
		for i := range fields {
			if fields[i] != balanceredpacketclaim.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if _q.withRedpacket != nil {
			_spec.Node.AddColumnOnce(balanceredpacketclaim.FieldRedpacketID)
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *BalanceRedPacketClaimQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(balanceredpacketclaim.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = balanceredpacketclaim.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range _q.modifiers {
		m(selector)
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (_q *BalanceRedPacketClaimQuery) ForUpdate(opts ...sql.LockOption) *BalanceRedPacketClaimQuery {
	if _q.driver.Dialect() == dialect.Postgres {
		_q.Unique(false)
	}
	_q.modifiers = append(_q.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return _q
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (_q *BalanceRedPacketClaimQuery) ForShare(opts ...sql.LockOption) *BalanceRedPacketClaimQuery {
	if _q.driver.Dialect() == dialect.Postgres {
		_q.Unique(false)
	}
	_q.modifiers = append(_q.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return _q
}

// BalanceRedPacketClaimGroupBy is the group-by builder for BalanceRedPacketClaim entities.
type BalanceRedPacketClaimGroupBy struct {
	selector
	build *BalanceRedPacketClaimQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *BalanceRedPacketClaimGroupBy) Aggregate(fns ...AggregateFunc) *BalanceRedPacketClaimGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *BalanceRedPacketClaimGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*BalanceRedPacketClaimQuery, *BalanceRedPacketClaimGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *BalanceRedPacketClaimGroupBy) sqlScan(ctx context.Context, root *BalanceRedPacketClaimQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// BalanceRedPacketClaimSelect is the builder for selecting fields of BalanceRedPacketClaim entities.
type BalanceRedPacketClaimSelect struct {
	*BalanceRedPacketClaimQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *BalanceRedPacketClaimSelect) Aggregate(fns ...AggregateFunc) *BalanceRedPacketClaimSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *BalanceRedPacketClaimSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*BalanceRedPacketClaimQuery, *BalanceRedPacketClaimSelect](ctx, _s.BalanceRedPacketClaimQuery, _s, _s.inters, v)
}

func (_s *BalanceRedPacketClaimSelect) sqlScan(ctx context.Context, root *BalanceRedPacketClaimQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
