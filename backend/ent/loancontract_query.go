// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/Wei-Shaw/sub2api/ent/loancontract"
	"github.com/Wei-Shaw/sub2api/ent/predicate"
	"github.com/Wei-Shaw/sub2api/ent/user"
)

// LoanContractQuery is the builder for querying LoanContract entities.
type LoanContractQuery struct {
	config
	ctx          *QueryContext
	order        []loancontract.OrderOption
	inters       []Interceptor
	predicates   []predicate.LoanContract
	withBorrower *UserQuery
	withLender   *UserQuery
	modifiers    []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the LoanContractQuery builder.
func (_q *LoanContractQuery) Where(ps ...predicate.LoanContract) *LoanContractQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *LoanContractQuery) Limit(limit int) *LoanContractQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *LoanContractQuery) Offset(offset int) *LoanContractQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *LoanContractQuery) Unique(unique bool) *LoanContractQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *LoanContractQuery) Order(o ...loancontract.OrderOption) *LoanContractQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryBorrower chains the current query on the "borrower" edge.
func (_q *LoanContractQuery) QueryBorrower() *UserQuery {
	query := (&UserClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(loancontract.Table, loancontract.FieldID, selector),
			sqlgraph.To(user.Table, user.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, loancontract.BorrowerTable, loancontract.BorrowerColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryLender chains the current query on the "lender" edge.
func (_q *LoanContractQuery) QueryLender() *UserQuery {
	query := (&UserClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(loancontract.Table, loancontract.FieldID, selector),
			sqlgraph.To(user.Table, user.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, loancontract.LenderTable, loancontract.LenderColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first LoanContract entity from the query.
// Returns a *NotFoundError when no LoanContract was found.
func (_q *LoanContractQuery) First(ctx context.Context) (*LoanContract, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{loancontract.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *LoanContractQuery) FirstX(ctx context.Context) *LoanContract {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first LoanContract ID from the query.
// Returns a *NotFoundError when no LoanContract ID was found.
func (_q *LoanContractQuery) FirstID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{loancontract.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *LoanContractQuery) FirstIDX(ctx context.Context) int64 {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single LoanContract entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one LoanContract entity is found.
// Returns a *NotFoundError when no LoanContract entities are found.
func (_q *LoanContractQuery) Only(ctx context.Context) (*LoanContract, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{loancontract.Label}
	default:
		return nil, &NotSingularError{loancontract.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *LoanContractQuery) OnlyX(ctx context.Context) *LoanContract {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only LoanContract ID in the query.
// Returns a *NotSingularError when more than one LoanContract ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *LoanContractQuery) OnlyID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{loancontract.Label}
	default:
		err = &NotSingularError{loancontract.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *LoanContractQuery) OnlyIDX(ctx context.Context) int64 {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of LoanContracts.
func (_q *LoanContractQuery) All(ctx context.Context) ([]*LoanContract, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*LoanContract, *LoanContractQuery]()
	return withInterceptors[[]*LoanContract](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *LoanContractQuery) AllX(ctx context.Context) []*LoanContract {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of LoanContract IDs.
func (_q *LoanContractQuery) IDs(ctx context.Context) (ids []int64, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(loancontract.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *LoanContractQuery) IDsX(ctx context.Context) []int64 {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *LoanContractQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*LoanContractQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *LoanContractQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *LoanContractQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *LoanContractQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the LoanContractQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *LoanContractQuery) Clone() *LoanContractQuery {
	if _q == nil {
		return nil
	}
	return &LoanContractQuery{
		config:       _q.config,
		ctx:          _q.ctx.Clone(),
		order:        append([]loancontract.OrderOption{}, _q.order...),
		inters:       append([]Interceptor{}, _q.inters...),
		predicates:   append([]predicate.LoanContract{}, _q.predicates...),
		withBorrower: _q.withBorrower.Clone(),
		withLender:   _q.withLender.Clone(),
		// clone intermediate query.
		sql:  _q.sql.Clone(),
		path: _q.path,
	}
}

// WithBorrower tells the query-builder to eager-load the nodes that are connected to
// the "borrower" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *LoanContractQuery) WithBorrower(opts ...func(*UserQuery)) *LoanContractQuery {
	query := (&UserClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withBorrower = query
	return _q
}

// WithLender tells the query-builder to eager-load the nodes that are connected to
// the "lender" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *LoanContractQuery) WithLender(opts ...func(*UserQuery)) *LoanContractQuery {
	query := (&UserClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withLender = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.LoanContract.Query().
//		GroupBy(loancontract.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *LoanContractQuery) GroupBy(field string, fields ...string) *LoanContractGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &LoanContractGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = loancontract.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.LoanContract.Query().
//		Select(loancontract.FieldCreatedAt).
//		Scan(ctx, &v)
func (_q *LoanContractQuery) Select(fields ...string) *LoanContractSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &LoanContractSelect{LoanContractQuery: _q}
	sbuild.label = loancontract.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a LoanContractSelect configured with the given aggregations.
func (_q *LoanContractQuery) Aggregate(fns ...AggregateFunc) *LoanContractSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *LoanContractQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !loancontract.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *LoanContractQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*LoanContract, error) {
	var (
		nodes       = []*LoanContract{}
		_spec       = _q.querySpec()
		loadedTypes = [2]bool{
			_q.withBorrower != nil,
			_q.withLender != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*LoanContract).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &LoanContract{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withBorrower; query != nil {
		if err := _q.loadBorrower(ctx, query, nodes, nil,
			func(n *LoanContract, e *User) { n.Edges.Borrower = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withLender; query != nil {
		if err := _q.loadLender(ctx, query, nodes, nil,
			func(n *LoanContract, e *User) { n.Edges.Lender = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *LoanContractQuery) loadBorrower(ctx context.Context, query *UserQuery, nodes []*LoanContract, init func(*LoanContract), assign func(*LoanContract, *User)) error {
	ids := make([]int64, 0, len(nodes))
	nodeids := make(map[int64][]*LoanContract)
	for i := range nodes {
		fk := nodes[i].BorrowerID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(user.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "borrower_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *LoanContractQuery) loadLender(ctx context.Context, query *UserQuery, nodes []*LoanContract, init func(*LoanContract), assign func(*LoanContract, *User)) error {
	ids := make([]int64, 0, len(nodes))
	nodeids := make(map[int64][]*LoanContract)
	for i := range nodes {
		if nodes[i].LenderID == nil {
			continue
		}
		fk := *nodes[i].LenderID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(user.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "lender_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (_q *LoanContractQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *LoanContractQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(loancontract.Table, loancontract.Columns, sqlgraph.NewFieldSpec(loancontract.FieldID, field.TypeInt64))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, loancontract.FieldID)
		for i := range fields {
			if fields[i] != loancontract.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if _q.withBorrower != nil {
			_spec.Node.AddColumnOnce(loancontract.FieldBorrowerID)
		}
		if _q.withLender != nil {
			_spec.Node.AddColumnOnce(loancontract.FieldLenderID)
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *LoanContractQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(loancontract.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = loancontract.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range _q.modifiers {
		m(selector)
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (_q *LoanContractQuery) ForUpdate(opts ...sql.LockOption) *LoanContractQuery {
	if _q.driver.Dialect() == dialect.Postgres {
		_q.Unique(false)
	}
	_q.modifiers = append(_q.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return _q
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (_q *LoanContractQuery) ForShare(opts ...sql.LockOption) *LoanContractQuery {
	if _q.driver.Dialect() == dialect.Postgres {
		_q.Unique(false)
	}
	_q.modifiers = append(_q.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return _q
}

// LoanContractGroupBy is the group-by builder for LoanContract entities.
type LoanContractGroupBy struct {
	selector
	build *LoanContractQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *LoanContractGroupBy) Aggregate(fns ...AggregateFunc) *LoanContractGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *LoanContractGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*LoanContractQuery, *LoanContractGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *LoanContractGroupBy) sqlScan(ctx context.Context, root *LoanContractQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// LoanContractSelect is the builder for selecting fields of LoanContract entities.
type LoanContractSelect struct {
	*LoanContractQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *LoanContractSelect) Aggregate(fns ...AggregateFunc) *LoanContractSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *LoanContractSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*LoanContractQuery, *LoanContractSelect](ctx, _s.LoanContractQuery, _s, _s.inters, v)
}

func (_s *LoanContractSelect) sqlScan(ctx context.Context, root *LoanContractQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
